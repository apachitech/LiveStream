services:
  - type: web
    name: livewave-stream-sfu
    env: docker
    plan: free
    region: oregon # Options: oregon, frankfurt, ohio, singapore
    dockerfilePath: ./Dockerfile
    healthCheckPath: /api/rooms
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
